server:
  port: 7000


eureka:
  instance:
    hostname: locahost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://locahost:${server.port}/eureka/

spring:
  application:
    name: server